"""Shared project library (scoring persistence, filters, website prompt helpers, v10 pipeline package)."""

# Re-export nothing by default; use ``lib.stock_rally_v10`` or ``lib.scoring_persist`` explicitly.
